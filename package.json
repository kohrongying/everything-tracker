{
  "name": "everything-tracker",
  "version": "1.0.0",
  "description": "Everything Tracker - Full stack application",
  "scripts": {
    "cdk:synth": "cd infra && npm run build && cdk synth",
    "cdk:deploy": "cd infra && npm run build && cdk deploy",
    "cdk:diff": "cd infra && npm run build && cdk diff",
    "cdk:bootstrap": "cd infra && cdk bootstrap"
  },
  "workspaces": [
    "frontend",
    "infra"
  ],
  "devDependencies": {
    "aws-cdk": "^2.241.0"
  }
}